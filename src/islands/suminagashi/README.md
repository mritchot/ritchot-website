# suminagashi

Placeholder — built in Phase 4.

Homepage-only canvas ink-marbling hero, loaded `client:idle`. First-party
vanilla TS, dependency-free at runtime, removable without breakage. A static
frozen-marble SVG poster paints first and is the permanent state for no-JS,
`prefers-reduced-motion`, and island failure. Decorative (`aria-hidden`);
the simulation pauses when the page is hidden.
